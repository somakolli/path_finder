name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: mkdir
      run: mkdir build
    - name: cd
      run: cd build  
    - name: ls
      run: ls
    - name: cmake
      run: cmake ..
    - name: make
      run: make
